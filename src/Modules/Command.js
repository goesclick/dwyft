/**
 * @file The Command class.
 */

/**
 * @export
 * @class Command Represents a command to be run from the shell.
 */
export default class Command {

  /**
   * Creates a new command to be run.
   * @constructor
   * @type {object} options The options for the command.
   * @memberof Command
   */
  constructor(options, optionsAreEqualLength) {}

  /**
   *
   *
   * @memberof Command
   */
  iterativeStrategy() {}

  /**
   *
   *
   * @memberof Command
   */
  recursiveStrategy() { }

  /**
   *
   *
   * @memberof Command
   */
  run() { }

}
